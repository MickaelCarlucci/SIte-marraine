<!-- modal -->
<div id="myModal" class="modal">
  <div class="modal-content">
      <span class="close" id="close-desc">&times;</span>
      <form id="formDesc" action="/updateArrayDesc/<%= arrays.id %>" method="post" data-id="<%= arrays.id %>">
          <!-- Champ caché pour l'ID du tableau -->
          <input type="hidden" name="id">
          <textarea name="description" id="description" cols="100" rows="20" placeholder="<%= arrays.description %>"></textarea>
          <button type="submit">Modifier</button>
      </form>
  </div>
</div>